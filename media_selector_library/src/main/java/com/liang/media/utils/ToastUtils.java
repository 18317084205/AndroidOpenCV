package com.liang.media.utils;import android.content.Context;import android.os.Handler;import android.os.Looper;import android.widget.Toast;public class ToastUtils {    private static Toast sToast;    private static Handler sHandler = new Handler(Looper.getMainLooper());    private static Runnable sRunnable = new Runnable() {        public void run() {            sToast.cancel();        }    };    public static void showToast(Context context, String text) {        sHandler.removeCallbacks(sRunnable);        if (null != sToast) {            sToast.cancel();            sToast.setText(text);        } else {            sToast = Toast.makeText(context.getApplicationContext(), text, Toast.LENGTH_SHORT);        }//        sHandler.postDelayed(sRunnable, 3000);        sToast.show();    }    public static void showToast(Context context, int strId) {        showToast(context.getApplicationContext(), context.getString(strId));    }}